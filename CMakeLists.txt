cmake_minimum_required(VERSION 3.10)
project(beton)
add_compile_options(-Wall -Wextra -Werror -Wpedantic)
add_link_options()
add_executable(beton src/beton.c)
install(TARGETS beton DESTINATION /usr/bin)
